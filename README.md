# PopPop base site installer

A basic site install recipe based on the Standard recipe from Drupal core.

- Currently using Drupal 10
- Does not expect to be an LGD site but installs the LGD base theme
